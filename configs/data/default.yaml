
_target_: src.dataset.loader.DPPSFDataModule

LDS_cfg:
  use_LDS: true
  LDS_step: 0.5
  LDS_ks: 5
  LDS_sigma: 2

batch_size: 1  # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 4
pin_memory: False
train_val_split: [0.9, 0.1]